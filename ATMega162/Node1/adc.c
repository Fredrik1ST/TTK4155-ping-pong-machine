#include <inttypes.h> // Needed for uint16_t

/**
 * Initialize the Analog-Digital-Converter (ADC)
 */
void adc_init(){
	
}

/** TODO
 * Triggers a reading from the ADC
 *
 * @return 8-bit value from ADC
 */
uint8_t adc_read(uint8_t channel){ // volatile
	
}

/** TODO
 * Calibrates the ADC (sets current position as origo)
 *
 */
void pos_calibrate(){ // volatile
	
}